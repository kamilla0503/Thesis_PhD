\chapter{Модель Изинга на динамических структурах}
\label{ch:ch3}

В данной главе приведены численные результаты исследования модели Изинга на динамических структурах (полимерных цепочках, заданных блужданиями без самопересечений на регулярной решетке). 
В работе анализируются геометрические (переход клубок–глобула) и магнитные  критические свойства системы. 
Основные результаты прямого численного моделирования методом Монте-Карло, включая качественную картину переходов и ключевые выводы по критическим параметрам, опубликованы в работе \cite{faizullina2021critical1}.

Дополнительно, по сравнению с опубликованной работой  \cite{faizullina2021critical1}, в тексте диссертации приведены расширенные данные и уточняющий анализ в узкой окрестности критических областей, полученные с использованием параллельного обмена репликами и мультигистограммного перевзвешивания (описаны в разделе \ref{MCMC_effect}). 
Эти методы позволяют восстановить гладкие зависимости наблюдаемых от параметра взаимодействия, повысить точность определения критических точек и проверить устойчивость выводов, полученных на «сырых» данных прямого моделирования.


 
 \section{2D}
 
 В данном разделе представлены результаты исследования модели Изинга на блужданиях без самопересечений на двумерной квадратной решётке. Последовательно анализируются два аспекта критического поведения системы: конформационный переход клубок-глобула (раздел \ref{subsection:ising_2d_struct}) и магнитный переход парамагнетик-ферромагнетик (раздел \ref{ising:magnet}). 
 Для каждого из них определяются критические точки, оцениваются показатели и устанавливается род фазового перехода. 
  
 \subsection{Геометрический переход}
 \label{subsection:ising_2d_struct}
 
 Для изучения перехода клубок-глобула в модели Изинга на блужданиях без  самопересечений используются геометрические характеристики: средний радиус \eqref{endtoend} и радиус вращения \eqref{gyration}. 

 \textbf{Оценка критического показателя $\nu$}.   
Критический показатель оценивается из степенного закона \eqref{r_scale}.   Предварительная оценка универсального параметра $\nu$ проводится с помощью  линейной аппроксимации логарифмированных данных квадратов радиусов из степенного закона $	\langle R_N^2 \rangle \sim N^{2 \nu } $:  

  \begin{equation}
	\label{r_scale_theta_log}
 \ln \langle R_N^2 \rangle  = 2 \nu \ln N +b.  
\end{equation} 


Параметры $(2\nu, b)$ подбираются через решение линейной регрессии в
лог-лог-пространстве с  учетом дисперсии данных. 

   \begin{figure}
	\centering
	\includegraphics[scale=0.30]{images/Ising_2D_re2e_nu.png}
	\includegraphics[scale=0.30]{images/Ising_2D_rg_nu.png}
	\caption{  Оценка критического показателя $\nu$ \eqref{r_scale} по численным данным среднего радиуса \eqref{endtoend} (слева) и радиуса вращения \eqref{gyration} (справа).    }
	\label{fig:ising_2d_nu_estimations}
\end{figure}

На графике  \ref{fig:ising_2d_nu_estimations} показаны полученные оценки для универсального показателя $\nu$ в зависимости от параметра взаимодействия  $J$. Оценки, полученные по среднему радиусу и радиусу вращения, совпадают в пределах статистической погрешности. 

Для двумерной решетки, в области высокой температуры (малых значений $J$), значение показателя $\nu \rightarrow \frac{3}{4}$, что соответствует блужданию без самопересечений без взаимодействия\eqref{nur} --- режим сильного растворителя.   

В интервале значений $J \in [0.82; 0.85]$ показатель достигает универсального значения \eqref{nu_theta} (на графике показано синей горизонтальной линией), характерного для перехода клубок-глобула гомополимерной модели на квадратной решетке  (см.~\ref{homopolymer_2d}).

В области высоких значений $J > 0.85$ показатель падает $\nu \rightarrow \frac{1}{2}$, что соответствует глобулярному режиму  (см.~\ref{globular}).
Получение заниженной оценки индекса $\nu <  \frac{1}{2}$ объясняется конечно-размерными эффектами. 

Предварительный анализ  геометрических характеристик не противоречит предположению, что  модель Изинга на блужданиях без самопересечений испытывает классический переход клубок-глобула.

\textbf{Точка структурного перехода}. 
В точке фазового перехода клубок-глобула средний размер полимерной цепочки (определенный через средний радиус/радиус вращения)
подчиняется следующему закону \cite{Scaling_concepts}:
   \begin{equation}
	\label{r_scale_theta}
 	\langle R_N^2 \rangle = N ^ {2 \nu _ {\Theta}} \mathcal{F} [  (J - J_{\Theta}) N ^ { 1 / \nu_c}] =  N ^ {2 \nu_ {\Theta}} \mathcal{F} [  (J - J_{\Theta}) N ^ { \phi_ {\Theta} }] , 
\end{equation} 
где  $\mathcal{F} [  (J - J_{\Theta}) N ^ { 1 / \nu_c}]$ --- универсальная  безразмерная  функция (crossover function). 
Показатель $ \phi_ {\Theta}$ ---  это «кроссоверный показатель»,  характеризующий,  как быстро сужается критическая область при росте размера  цепочки. 

В критической точке $J = J_{\Theta} $ безразмерная величина 
$  \mathcal{R} (N, J)= 	\langle R_N^2 \rangle / N ^ {2 \nu _ {\Theta}} $  не зависит от размера системы $N$. 
Следовательно, кривые $  \mathcal{R} (N, J)  $ в точке фазового перехода клубок-глобула пересекаются в одной точке для всех  $N$. 

%Это позволит нам определить точку геометрического перехода. 

   \begin{figure}[ht!]%
	\centering
	\includegraphics[scale=0.25]{images/Ising_2D_rscaling_short_fixed.png}
	\includegraphics[scale=0.25]{images/Ising_2D_rscaling_long_fixed.png}
	\includegraphics[scale=0.25]{images/SI_ISing_2D_R2_scaled_nu057_reweight.png}
	\includegraphics[scale=0.25]{images/SI_ISing_2D_R2_scaled_nu057_reweight_zoomin.png}
	\caption{ Масштабированный средний радиус $\langle R_N^2\rangle/N^{2\nu_\Theta}$ (при фиксированном $\nu_\Theta$, см.~\eqref{nu_theta}) как функция от $J$. 
	Верхний ряд: результаты прямого численного моделирования методом Монте-Карло при фиксированных значениях $J$ (описан в разделе  \ref{MCMC}).
	Нижний ряд: данные в узкой окрестности $\Theta$-точки, полученные методом параллельного темперинга с мультигистограммным перевзвешиванием для коротких и длинных цепочек (описан в разделе  \ref{MCMC_effect}). Правая колонка: увеличенный фрагмент области пересечения кривых радиусов. 
	 }
	\label{fig:ising_2d_rscaling}
\end{figure}
 

Для оценки точки перехода клубок-глобула мы используем данные среднего радиуса, отмасштабированного с помощью критического показателя  \eqref{nu_theta}. Отмасштабированные данные изображены на графике \ref{fig:ising_2d_rscaling}. 

Поиска точки пересечения кривых средних радиусов и получения количественной  оценки критической точки $J_{\Theta}$ производится методом  парных регрессий ( описан в разделе \ref{subsection:paired_regression}). 
Из данных прямого численного моделирования, используется поднабор $N_{fixed}=5000$ и $N_i \in [1000; 7000]$, что позволяет использовать для анализа данные достаточно длинных цепочек,  при этом величина погрешностей данных моделирования остаётся приемлемой. В результате получена  следующая оценка:
   \begin{equation}
	\label{ising_2d_J_theta}
	J_{\Theta} \approx 0.833(1). 
\end{equation} 

Переход клубок-глобула в двумерной полимерной цепочке с Изинговым взаимодействием является непрерывным фазовым переходом второго рода. 

\begin{figure}[t]
	\centering
	\includegraphics[scale=0.25]{images/Ising_2D_R2_collapse_auto_fixed.png}
		\includegraphics[scale=0.25]{images/Ising_2D_R2_collapse_auto_reweight.png}
	\caption{Коллапс кривых средних радиусов $R^2/N^{2\nu_\Theta}$ при зафиксированном показателе $\nu_\Theta=4/7$ в координатах
		$X=(J-J_\Theta)N^{\phi_\Theta}$.
		Параметры коллапса, полученные перебором: $J_\Theta=0.832150$, $\phi_\Theta^{\mathrm{eff}}=0.73913$.
		График слева: данные прямого численного моделирования, которые использовались для подбора параметра коллапса. График справа:  коллапс для данных в узкой окрестности $\Theta$-точки, полученных  с использованием обмена реплик и мультигистограммным перевзвешиванием. 
 }
	\label{fig:R2_collapse}
\end{figure}


Формула \eqref{r_scale_theta} приводит к естественным безразмерным переменным \cite{G}
\begin{equation}
	Y(N,J) \equiv \frac{R^2(N,J)}{N^{2\nu_\Theta}},
	\qquad
	X(N,J;J_\Theta,\phi_\Theta) \equiv (J-J_\Theta)\,N^{\phi_\Theta}.
	\label{eq:XY_defs}
\end{equation}
При корректных параметрах $(J_\Theta,\phi_\Theta)$ кривые $Y(N,J)$ для разных $N$ вблизи структурного перехода должны накладываться на единую кривую.  
 Для получения максимально согласованного коллапса можно использовать процедуру перебора параметров на равномерной сетке по $J_\Theta$ и $\phi_\Theta$.  Оценка ошибок при такой процедуре не производится, это обобщение визуальной оценки, опубликованной в работе  \cite{faizullina2021critical1}. 
  Для поиска коллапса, значение универсального показателя фиксируется: $\nu_\Theta = 4/7$   \eqref{nu_theta}, так как согласно предыдущим оценкам этот критический показатель наследуется от классического гомополимера.  
Для каждой длины цепочки $N$ строится
пара $(X_i^{(N)},Y_i^{(N)})$ --- дискретное приближение кривой $Y^{(N)}$,
после чего все кривые интерполируются на общую сетку $X=\{x_k\}$.
Качество коллапса оценивается взвешенной дисперсией:
\begin{equation}
	\mathcal{C}(J_\Theta,\phi_\Theta)
	\;=\;
	\sum_{k}
	\sum_{N}
	w_k^{(N)}
	\Bigl[\,Y^{(N)}(x_k) - \overline{Y}(x_k)\Bigr]^2,
	\qquad
	w_k^{(N)} = \frac{1}{\bigl(\sigma^{(N)}(x_k)\bigr)^2},
	\label{eq:collapse_cost}
\end{equation}
где $\overline{Y}(x_k)$ --- взвешенное среднее по $N$ в узле $x_k$.
 
 
 На рисунке \ref{fig:R2_collapse} показаны  полученные результаты для коллапса кривых при значениях параметров $J_\Theta=0.832150$, $\phi_\Theta^{\mathrm{eff}}=0.73913$. 
 Кривые для разных $N$ практически накладываются друг на друга. 
 Значение $J_\Theta$, полученное поиском максимально согласованного коллапса,  согласуется с оценками по пересечениям \eqref{ising_2d_J_theta} при фиксированном $\nu_\Theta$. 
 
 
 Отметим, что полученное значение $\phi_\Theta^{\mathrm{eff}}\approx 0.74$ заметно выше теоретического трикритического значения $3/7\simeq 0.4286$. Однако, завышенные оценки при численном моделировании были также получены и  в более простой модели без спинов --- для классического гомополимера $\phi_\Theta^{\mathrm{eff}}\approx 0.479(6)$  \cite{Caracciolo_2011} при меньшем отклонении. 
 
 \subsubsection{Уточнение оценок по данным параллельного темперинга и мультигистограммного перевзвешивания}.
 В предыдущем подразделе точка $\Theta$-перехода и критические показатели оценивались по прямому численному моделированию при фиксированных значениях $J$, результаты анализа такого моделирования опубликованы в \cite{faizullina2021critical1}.
 Для проверки надежности выводов и повышения точности оценок в узкой окрестности перехода мы дополнительно используем данные, полученные методом параллельного темперинга с мультигистограммным перевзвешиванием.
 Этот подход даёт гладкую зависимость наблюдаемых от $J$ в заданном окне, что позволяет более надёжно находить точки пересечения масштабированных кривых и  повторить анализ коллапса в переменных $X=(J-J_\Theta)N^\phi$, $Y=\langle R_N^2\rangle/N^{2\nu_\Theta}$.
 Тем самым мы получаем независимую проверку результатов о геометрическом переходе и уточняем значения параметров $J_\Theta$ и $\phi=1/\nu_c$.
 
 
    \textbf{Оценка точки фазового структурного перехода с помощью парных регрессий}. 
 
 Оценки точек пересечения, полученные на данных мультигистаграммного перевзвешивания, показаны в таблице \ref{tab:crossings_reweight} и на графике \ref{fig:ising_si_2d_cross}. 
 Парные пересечения кривых $Y_N(J)$ для разных длин цепочки сосредоточены в узкой области
 $J \approx 0.8325$, что подтверждает наличие перехода клубок-глобула.
 
Усреднение по полученным оценкам  даёт
 \[
 J_\Theta = 0.83252(13).
 \]
 Система чувствительна к конечно-размерным
 поправкам, поэтому эффективную экстраполяцию по $N_{\mathrm{eff}}$ следует использовать
 как иллюстрацию, а не как единственную финальную оценку.  
 
 \begin{table}[t]
 	\centering
 	\small
 	\begin{tabular}{lcccc}
 		\toprule
 		$N_1$--$N_2$ & $N_{\mathrm{eff}}=\sqrt{N_1N_2}$ & $N_2/N_1$ & $J_{\times}$ &$ \langle R_N^2\rangle/N^{2\nu_\Theta}$  \\
 		\midrule
 		1000--2000 & 1414 & 2.000000 & 0.83319(10) & 1.6748(38) \\
 		1000--3000 & 1732 & 3.000000 & 0.832840(44) & 1.6918(31) \\
 		1000--5000 & 2236 & 5.000000 & 0.832509(32) & 1.7077(39) \\
 		2000--3000 & 2449 & 1.500000 & 0.83242(12) & 1.7342(36) \\
 		1000--8000 & 2828 & 8.000000 & 0.832316(38) & 1.7169(58) \\
 		2000--5000 & 3162 & 2.500000 & 0.832202(55) & 1.7511(43) \\
 		3000--5000 & 3873 & 1.666667 & 0.832068(71) & 1.7702(36) \\
 		2000--8000 & 4000 & 4.000000 & 0.832021(59) & 1.7651(61) \\
 		3000--8000 & 4899 & 2.666667 & 0.831872(66) & 1.7903(57) \\
 		5000--8000 & 6325 & 1.600000 & 0.83150(21) & 1.8546(63) \\
 		\bottomrule
 	\end{tabular}
 	\caption{Точки пересечения кривых отмасштабированного радиуса $ \langle R_N^2\rangle/N^{2\nu_\Theta}$, найденные методом парных регрессий на данных мультигистограммного перевзвешивания. }
 	\label{tab:crossings_reweight}
 \end{table}
   
 
 
     \begin{figure}%[ht!]
 	\centering
 	\includegraphics[scale=0.95]{images/theta_crossings_extrapolation_SI_Ising_2D.png}
 	\caption{ Эффективная экстраполяция точек пересечения $J_\times$ масштабированных радиусов для 
 		от эффективного размера пары $N_{\mathrm{eff}}^{-0.70}$, где
 		$N_{\mathrm{eff}}=\sqrt{N_1N_2}$.
 		Горизонтальная пунктирная линия соответствует сводной оценке $J_\Theta$,
 		полученной по всем парным пересечениям.   }
 	\label{fig:ising_si_2d_cross}
 \end{figure}
 
 
 
  \textbf{Оценка  показателя $\phi_\Theta  $  по численным производным}. 
      
Оценку кроссоверного индекса $\phi_\Theta  $  можно провести используя данные производных кривых радиусов. Вводится  безразмерная величина $ \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}} $
  для которой в окрестности критической $\Theta$-точки верна скейлинговая форма:
  \begin{equation}
  \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}   \equiv  \mathcal{F}\!\left(x\right),\qquad x=(J-J_\Theta)N^\phi _\Theta.
  \end{equation}
  Тогда производная по параметру взаимодействия $J$ имеет такой вид:
  \begin{equation}
  	 \mathcal{F}'(x)\,\frac{\partial x}{\partial J}
  	= \mathcal{F}'(x)\,N^{\phi_\Theta} .
  \end{equation}
  В критической точке $J=J_\Theta$ (то есть при $x=0$)  используется следующий скейлинг: 
  \begin{equation}
     \mathcal{F}'(0)\,N^{\phi_\Theta} \propto N^\phi .
  \end{equation}
  Следовательно, показатель $\phi$ можно извлечь из следующей логарифмической регрессии:
  \begin{equation}
  	\ln\!\left|\left.\frac{\partial    (  \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}    )   }{\partial J}\right|_{J_\Theta}\right|
  	= \phi_\Theta  \,\ln N + \mathrm{const},
  \end{equation}
  где $\partial (  \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}    )   /\partial J$ оценивается численно (конечными разностями
  вблизи $J_\Theta$), а затем проводится линейная
  аппроксимация в координатах $\bigl(\ln N,\ \ln|\partial_J  (  \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}    )    |\bigr)$ с учётом ошибок.
 
    Показатель $\phi_\Theta $ можно оценить методом производных по данным, полученным с помощью параллельного темперинга и мультигистограммного перевзвешивания.
 Причина практическая: для «сырых» (неперевзвешенных) данных численная оценка производной
 $\partial (  \frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}    )   /\partial J$ (через конечные разности) оказывается слишком шумной,
 и ошибки быстро растут при увеличении $N$.
 Поэтому для прямых данных мы ограничиваемся качественной оценкой параметров из  коллапса кривых отмасштабированного радиуса (рисунок  \ref{fig:R2_collapse}), 
 а в данном разделе  извлекаем количественную оценку $\phi$  из гладких отмашстабированных кривых  $\frac{\langle R_N^2\rangle}{N^{2\nu_\Theta}}    $, восстановленных перевзвешиванием.
 
 
 
 Результаты оценки кроссоверного показателя $\phi_\Theta$ по производным перевзвешенных данных
 приведены в таблице \ref{tab:phi_estimates} для двух диапазонов длин цепочек.
 
 \begin{table}[ht]
 	\centering
 	\small
 	\begin{tabular}{lcc}
 		\toprule
 		Метод & $N\leqslant 8000$ & $N< 8000$ \\
 		\midrule
 		Парные пересечения, $J_\Theta$
 		& $0.83231(11)$ & $0.83252(13)$ \\
 		Экстраполяция парных пересечений, $J_\Theta$
 		& $0.830988(80)$ & $0.83098(12)$ \\
 		Производная $\partial   ( \langle R_N^2\rangle/N^{2\nu_\Theta}) /\partial J$, $\phi_\Theta$
 		& $0.5966(18)$ & $0.6671(23)$ \\
 		Производная $\partial\ln R^2/\partial J$, $\phi_\Theta$
 		& $0.6149(18)$ & $0.6856(23)$ \\
 		Коллапс (перебор сетки параметров), $\phi_\Theta$
 		& $0.650$ & $0.706$ \\
 		\bottomrule
 	\end{tabular}
 	\caption{Оценки параметров $\Theta$-перехода при различных диапазонах длин цепочек.}
 	\label{tab:phi_estimates}
 \end{table}
 
 
 Оценки кроссоверного показателя $\phi_\Theta$, напротив, обнаруживают систематическую
 зависимость от диапазона длин: включение $N=8000$ снижает $\phi$ на $\Delta\phi\sim 0.07$.
 
 
 Это расхождение можно объяснить двумя факторами.
 Во-первых,   статистическая
 погрешность производной $\partial Y/\partial J$ для наибольшей длины цепочки
 максимальна вследствие более узкого окна перевзвешивания.
Во-вторых, на эффективный показатель заметно влияют конечно-размерные поправки вида
$\phi_{\mathrm{eff}}=\phi + a,N^{-\Delta}+\dots$,
причём их знак и величина зависят от того, какой диапазон длин цепей $N$ используется при обработке данных. Для высокоточного количественного анализа таких поправок нужно собрать значительно больше данных компьютерного моделирования для длинных цепочек $ N > \ge 2000$. 


 
Оценки параметров $\Theta$-перехода на перевзвешенных данных согласуются с результатами, полученными на данных прямого численного моделирования. 
В частности, значение $J_\Theta=0.833907(19)$, полученное из пересечений перевзвешенных кривых,
совместимо с прежней оценкой $J_\Theta\approx 0.833(1)$, полученной методом парных регрессий на сырых данных
(см.~\eqref{ising_2d_J_theta}), и попадает в тот же интервал $J\in[0.82;0.85]$, где наблюдается поведение,
характерное для $\Theta$-режима. 
Сеточный поиск параметров коллапса также даёт близкое значение
$J_\Theta=0.834650$, что подтверждает устойчивость определения критической точки.

Для кроссоверного индекса получено $\phi_\Theta=0.735$ (перебор сетки параметров для поиска коллапса), что согласуется с ранее найденной
эффективной оценкой $\phi_\Theta^{\mathrm{eff}}=0.73913$ (рис.~\ref{fig:R2_collapse}) в пределах ожидаемых
систематических погрешностей процедуры коллапса. 
Оценка, извлечённая из производных по перевзвешенным данным,
$\phi_\Theta=0.6964(23)$ оказывается ниже значений. %, что приводит к более высокой величине
%$\nu_c=1/\phi$ по сравнению с $\nu_c\simeq 1/0.74$. 
% Такое расхождение естественно связать с тем, что $\phi_\Theta$ остаётся эффективным показателем на конечных длинах цепочек, а производный метод более чувствителен к конечно-размерным поправкам и к выбору окна по $J$ и значения $J_\Theta$.
В целом новая серия оценок подтверждает сделанный выше вывод о завышенном значении $\phi_\Theta^{\mathrm{eff}}$
по сравнению с трикритическим значением $3/7$. 

 
 \subsection{Магнитный переход}
 \label{ising:magnet}
 
 \textbf{Намагниченность}.   
 Численные данные второго момента намагниченности в зависимости от константы $J$ представлены на графике \ref{fig:ising_2d_mags}. При малых значениях $J$ система находится в парамагнитном состоянии (также предел $J \rightarrow 0$ соответствует модели БСП без взаимодействия  \ref{homopolymer_2d} ). 
 
 В интервале $J\in  [0.82;0.85] $ наблюдается подъём кривых второго момента намагниченности --- в этой области происходит фазовый переход из неупорядоченного (парамагнитного) режима в упорядоченный (ферромагнитный). С увеличением $N$ подъем становится более резким, а точка перегиба сосредоточивается около  значения $J\approx 0.835$. 
 
   \begin{figure}[ht!]
 	\centering
 	\includegraphics[scale=0.25]{images/Ising_2D_rg2_m2_short_fixed.png}
 	\includegraphics[scale=0.25]{images/Ising_2D_rg2_m2_long_fixed.png}
 	\caption{ Второй момент намагниченности    \eqref{ising_mean_magnetization} как функция от  $J$.     }
 	\label{fig:ising_2d_mags}
 \end{figure}
  
  
   \textbf{Энергия}.  
  
      \begin{figure}
   	\centering
   	\includegraphics[scale=0.25]{images/Ising_2D_rg2_e1_short_fixed.png}
   	\includegraphics[scale=0.25]{images/Ising_2D_rg2_e1_long_fixed.png}
   	%\includegraphics[scale=0.18]{Images/state_example2.png}
   	\caption{ Первый момент энергии на спин как функция от  $J$.     }
   	\label{fig:ising_2d_energy}
   \end{figure} 
     
     На рисунке \ref{fig:ising_2d_energy} показано, как средняя энергия на спин  монотонно убывает с ростом энергии взаимодействия $J$. 
     Так, энергетические данные подтверждают магнитный критический переход, согласуясь с поведением второго момента намагниченности.
        
        
        
    \textbf{Теплоемкость}. 
     Для продолжения анализа фазового магнитного перехода рассмотрим еще одну физическую величину --- теплоемкость: 
    \begin{equation}
    	\label{eq:capacity_heat}
    	C_N(J) \;=\; \frac{\partial \langle E\rangle}{\partial J}
    	\;=\; \mathrm{Var}(E) \equiv \langle E^2\rangle - \langle E\rangle^2,
    \end{equation}
    
    Удобно работать с удельной величиной $c_N(J)=C_N(J)/N$. При переходе первого рода пик удельной теплоемкости маcштабируется следующим образом: 
    \(
    c_N^{\max}\sim N^{\alpha} \sim N. 
        \)   
    При  непрерывном переходе:
    \(
    c_N^{\max}
    \)
    растёт сублинейно (логарифмически или степенной  показатель $\alpha<1$) \cite{PhysRevB.34.1841} \cite{Landau_Binder_2021} \cite{PhysRevB.43.3265}. 
    
        \begin{figure}
    	\centering
    	\includegraphics[scale=0.25]{images/ising_heat_c_vs_J_fixed.png}
    	\includegraphics[scale=0.25]{images/ising_heat_cmax_scaling_fixed.png}
    	\caption{ Численные данные для удельной теплоемкости \eqref{eq:capacity_heat}. На левом графике  в качестве ошибок показана верхняя граница. Оценки пиков теплоемкости показаны черными точками для каждой длины цепочки $N$. }
    	\label{fig:C_vs_J_short}
    \end{figure} 
        
        
      Полученные кривые  удельной теплоемкости \eqref{eq:capacity_heat}  при компьютерном моделировании  показаны на графике  \ref{fig:C_vs_J_short}. 
      Максимум удельной теплоемкости монотонно растёт с $N$, но его рост 
      существенно медленнее линейного --- оценка параметра  $\alpha \approx 0.36 \pm 0.05$. Такое поведение согласуется с непрерывным переходом. 
        
        
 \textbf{Кумулянты Биндера для намагниченности}.  Для изучения фазового магнитного перехода рассмотрим кумулянт Биндера четвертого порядка для намагниченности  \eqref{binder_cumulant}  \cite{PhysRevLett.47.693}.  
 
       \begin{figure}
 	\centering
 	\includegraphics[scale=0.25]{images/Ising_2D_U4_short_fixed.png}
 	\includegraphics[scale=0.25]{images/Ising_2D_U4_long_fixed.png}
 	\includegraphics[scale=0.25]{images/SI_ISing_2D_binder_u3_reweight.png}
 	\caption{ Кумулянты Биндера \eqref{binder_cumulant}  как функция от  $J$.   Верхний ряд: результаты прямого численного моделирования методом Монте-Карло при фиксированных значениях $J$ (описан в разделе  \ref{MCMC}).
 		Нижний  ряд: данные в узкой окрестности $\Theta$-точки, полученные методом параллельного темперинга с мультигистограммным перевзвешиванием (для коротких и длинных цепочек).   }
 	\label{fig:ising_2d_U4}
 \end{figure} 
 
 На графике  \ref{fig:ising_2d_U4}  показаны полученные кривые кумулянтов. В интервале $J \in [0.83; 0.85]$ кривые кумулянтов для разных длин $N$ пересекаются. Такое поведение согласуется с картиной
 непрерывного (второго рода) магнитного перехода. 
  
  Воспользуемся методом парных регрессий (раздел \ref{subsection:paired_regression})  для оценки критической точки фазового перехода. Аналогично анализу структурного перехода \ref{subsection:ising_2d_struct}, возьмем данные для цепочек длины $N_{fixed}=5000$ и $N_i \in [1000; 9000]$. Получены следующие оценки для критических значений: 
  
   \begin{equation}
  	\label{Ising_2d_U4_cr} 
  	 U^{cr}_4	=  0.308,
  \end{equation}  
 
  \begin{equation}
  	\label{Ising_2d_J_cr} 
  J_{cr}	= 0.8340(5). 
  \end{equation}  
   
   \subsubsection{Уточнение оценок по данным параллельного темперинга и мультигистограммного перевзвешивания}. 
    
    
    \textbf{Метод производных}. 
    Дополнительно для каждой длины $N$ определяется  критическая точка $J^*(N)$ как положение максимального наклона кумулянта
    \begin{equation}
    	J^*(N):\quad \left|\frac{\partial U_4}{\partial J}(J;N)\right|\ \text{максимально}.
    \end{equation}
    Производная $\partial U_4/\partial J$ оценивается численно по гладкой зависимости $U_4(J;N)$, восстановленной методом перевзвешивания. 
    Полученные численные оценки представлены в таблице \ref{tab:binder_derivative_points}. 
   % (в реализации используется сглаживающий сплайн; погрешность $J^*(N)$ оценивается бутстрэпом).
%    Далее выполняется экстраполяция
%    \begin{equation}
%    	J^*(N)=J_c + B\,N^{-p},
%    \end{equation}
%    что даёт независимую оценку $J_{cr}$ и масштаба конечно-размерных поправок.
%    
%    

   \begin{table}
   	\caption{Критические точки $J^*(N)$, вычисленные по максимуму наклона $|\partial U_4/\partial J|$ .}
   	 	\centering 
   	\label{tab:binder_derivative_points}
   	\begin{tabular}{lll}
   		\toprule
   		N & $J_{cr}$ & $max|dU^{cr}_4/dJ| $\\
   		\midrule
1000 & 0.8326(69) & 14.8(66) \\
2000 & 0.8325(51) & 23(17) \\
3000 & 0.8340(47) & 28.7(74) \\
5000 & 0.8336(36) & 40(13) \\
8000 & 0.8324(48) & 51(11) \\
   		\bottomrule
   	\end{tabular}
   \end{table}
   
   
   \textbf{Парные регрессии на перевзвешенных данных}. 
   Метод парной регрессии (раздел \ref{subsection:paired_regression} ) применен на  перевзвешенных данных.  
   Выбрана фиксированная длина $N_{\mathrm{fixed}}=5000$, так как из полученных вычислительных данных это самая большая промоделированная система с приемлемыми статистическими ошибками. 
   Полученные численные оценки представлены в таблице \ref{tab:binder_crossings_fixed}. 
   
   
   \begin{table}
   	\caption{Точки пересечения кумулянтов Биндера $U_4(J)$ (фиксированное $N_{\mathrm{fixed}}=5000$).}
   		\centering 
   	\label{tab:binder_crossings_fixed}
   	\begin{tabular}{lll}
   		\toprule
   		$N_1 -- N_2$ & $J_{cr}$ & $U^{cr}_4$ \\
   		\midrule
5000--1000 & 0.83513(18) & 0.3382(39) \\
5000--2000 & 0.83486(31) & 0.3298(79) \\
5000--3000 & 0.83382(56) & 0.297(15) \\
   		\bottomrule
   	\end{tabular}
   \end{table}
   
   \textbf{Вывод по результатам анализа перевзвешенных данных}. 
   Перевзвешенные данные подтверждают наличие магнитного перехода в той же узкой области значений $J$, что и по результатам прямого компьютерного моделирования \eqref{Ising_2d_J_cr}. 
   Псевдокритические точки $J^*(N)$, определённые по максимуму наклона производной  $|\partial U_4/\partial J|$, лежат в интервале $0.8324\text{--}0.8340$.
   %и в пределах ошибок не демонстрируют заметного систематического дрейфа с ростом $N$. 
   Это даёт  надежную оценку критической области $J_{cr}\approx 0.833\text{--}0.834$, совместимую с ранее полученным значением $J_{cr}=0.8340(5)$ по данным прямого численного моделирования.
   
   Рост максимального наклона кумулянта Биндера с увеличением длины цепочки
   свидетельствует о прогрессирующем заострении критической области: от $14.8(66)$ при $N=1000$ до $51(11)$ при $N=8000$ .
   Такое поведение согласуется со сценарием непрерывного фазового перехода,
   для которого в рамках конечно-размерного масштабирования ожидается степенной рост
   $\partial U_4/\partial J$ в критической точке. В сочетании с монотонной формой кривых
   $U_4(J)$ и наличием их пересечения это поддерживает вывод о магнитном переходе второго рода \cite{PhysRevLett.47.693}. 
   
 
     Точки пересечения перевзвешенных кривых $U_4(J)$ для пар с $N_{\mathrm{fixed}}=5000$ в основном также сосредоточены вблизи $J\approx 0.834$. %, хотя разброс оценок здесь заметно выше, чем для  метода анализа численных производных. 
  %   В частности, пара $5000$--$3000$ даёт отклоняющееся значение $J_{cr}=0.83002(96)$ и существенно меньшее $U_4^{cr}$, что может указывать на . 
     
     %Поэтому по перевзвешенным данным критическую точку $J_{cr}$ можно считать определённой более надёжно, чем универсальное значение самого кумулянта $U_4^{cr}$.
     
     В целом, анализ перевзвешенных данных подтверждает устойчивость вывода о непрерывном магнитном переходе при
     \[
     J_{cr}\approx 0.834,
     \]
     причём эта точка совпадает с точностью до ошибок с областью структурного $\Theta$-перехода. 
     Следовательно, в двумерной модели Изинга на динамических структурах магнитное упорядочение и структурная перестройка цепочки происходят в одной и той же узкой критической области.
     
     
 \subsection{Распределение энергии.}
\begin{figure}[ht!]
 	\centering
 	\includegraphics[scale=0.25]{images/Ising_distrenergy_5000.png}
 	\includegraphics[scale=0.25]{images/Ising_distrenergy_7500.png}
 	\caption{  Распределения энергии на спин для цепочек длины  $N=5000$ (слева)  и  $N=7500$  (справа).}
 	\label{fig:ising_2d_distr_E}
 \end{figure} 
  Для дальнейшего изучения характера фазового перехода мы рассмотрим распределения энергии, нормированное длину цепочки $N$. На графике  \ref{fig:ising_2d_distr_E}  изображены распределения энергии, нормированные на спин, для цепочек длины  $N=5000$ (слева)  и  $N=7500$  (справа) в интервале, включающем полученные оценки критических точек \eqref{Ising_2d_J_cr}  и  \eqref{ising_2d_J_theta}. 
  Полученные формы распределений являются одномодальными, что соответствует непрерывному фазовому переходу. 
 
 
  \subsection{Вывод} 

 Оценки для магнитного перехода \eqref{Ising_2d_J_cr}   и перехода клубок-глобула \eqref{ising_2d_J_theta} согласуются в пределах погрешности.  
 
 Анализ геометрических характеристик показал, что структурный переход в двумерной полимерной цепочке c Изинговым взаимодействием является непрерывным переходом. Наши вычислительные результаты показывают, что при конформационном переходе критический показатель  $\nu=4/7$  наследуется от тета-полимера \eqref{nu_theta}. 
 
 Анализ термодинамических характеристик показал, что магнитный переход в системе является переходом второго рода. 
 